---
galaxy_info:
  author: MongoDB
  description: Installs the AWS Command Line Interface (CLI)
  license: Apache
  min_ansible_version: 2.8
  platforms:
    - name: Debian
      versions:
        - stretch
  galaxy_tags:
    - awscli
dependencies:
  - pip
